# Rust-Learning

This repository is for learning rust language.
#Below are the command used to install Rust:

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

cargo --version

cargo new <projectname>

cargo run

cargo run -q
